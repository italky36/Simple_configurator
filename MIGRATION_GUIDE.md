# Миграция: Добавление поддержки цветных вставок

## Описание изменений

Добавлена поддержка выбора изображений кофемашины для каждой комбинации цвета корпуса и цвета вставки.

### Изменения в базе данных

Добавлено новое поле `design_images` в таблицу `coffee_machines` для хранения изображений для каждой комбинации цветов.

### Структура данных

```json
{
  "белый": {
    "жёлтый": {
      "main_image": "URL или путь к изображению",
      "main_image_path": "Путь в Seafile",
      "gallery_folder": "Путь к папке с галереей"
    },
    "зелёный": {...},
    "красный": {...},
    "серый": {...},
    "синий": {...},
    "фиолетовый": {...}
  },
  "чёрный": {
    "жёлтый": {...},
    ...
  }
}
```

## Инструкции по миграции

### Вариант 1: Автоматическая миграция (рекомендуется)

Запустите скрипт миграции:

```bash
python scripts/add_design_images_column.py
```

### Вариант 2: Ручная миграция через SQL

Если у вас SQLite:

```bash
sqlite3 path/to/your/database.db "ALTER TABLE coffee_machines ADD COLUMN design_images TEXT;"
```

Для PostgreSQL:

```sql
ALTER TABLE coffee_machines ADD COLUMN design_images JSONB;
```

Для MySQL:

```sql
ALTER TABLE coffee_machines ADD COLUMN design_images JSON;
```

### Вариант 3: Пересоздание базы данных

Если база данных в разработке и можно начать заново:

```bash
# Удалите старую базу данных
rm -f *.db

# Создайте новую базу данных
python scripts/init_db.py
```

## Использование новой функции

### В админке

1. Откройте редактирование кофемашины
2. Прокрутите вниз до секции "Цвета дизайна (корпус × вставка)"
3. Раскройте аккордеоны для белого или чёрного корпуса
4. Для каждого цвета вставки можно настроить:
   - Главное фото
   - Папку с галереей
5. Используйте кнопку "Seafile" для выбора файлов из Seafile

### На фронтенде

Пользователи теперь могут:
1. Выбрать цвет корпуса (белый/чёрный)
2. Выбрать цвет вставки (жёлтый, зелёный, красный, серый, синий, фиолетовый)
3. Увидеть соответствующее изображение кофемашины для выбранной комбинации

## Структура файлов в Seafile

Рекомендуемая структура папок в Seafile:

```
📁 [Название кофемашины]
   └── 📁 [Модель каркаса]
         ├── 📁 белый
         │     ├── 📁 жёлтый
         │     │     ├── main.jpg
         │     │     └── 📁 gallery
         │     ├── 📁 зелёный
         │     ├── 📁 красный
         │     ├── 📁 серый
         │     ├── 📁 синий
         │     └── 📁 фиолетовый
         └── 📁 чёрный
               ├── 📁 жёлтый
               ├── 📁 зелёный
               ├── 📁 красный
               ├── 📁 серый
               ├── 📁 синий
               └── 📁 фиолетовый
```

## API изменения

### Новые параметры в `/api/coffee-machines`

- `frame_color` (опционально) - цвет корпуса (белый/чёрный)
- `insert_color` (опционально) - цвет вставки

Пример:
```
GET /api/coffee-machines?include_gallery=true&frame_color=белый&insert_color=жёлтый
```

При указании обоих параметров API вернет изображения для конкретной комбинации цветов из `design_images`.
