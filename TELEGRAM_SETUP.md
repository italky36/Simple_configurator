# Настройка Telegram бота для уведомлений о заявках

## 1. Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например, "Coffee Machine Leads Bot")
   - Введите username бота (например, "coffee_leads_bot")
4. BotFather выдаст вам **токен бота** вида: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
5. Сохраните этот токен - он понадобится для настройки

## 2. Получение Chat ID

### Вариант A: Для личных сообщений
1. Найдите своего бота в Telegram по username
2. Нажмите "Start" или отправьте любое сообщение боту
3. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
   (замените `<YOUR_BOT_TOKEN>` на ваш токен)
4. Найдите в ответе значение `"chat":{"id":123456789}` - это ваш Chat ID

### Вариант B: Для группы/канала
1. Создайте группу или канал в Telegram
2. Добавьте вашего бота в группу/канал как администратора
3. Отправьте любое сообщение в группу/канал
4. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
5. Найдите в ответе `"chat":{"id":-100123456789}` - это Chat ID группы/канала
   (для групп и каналов ID начинается с `-100`)

## 3. Настройка .env

Добавьте следующие строки в файл `.env`:

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

Замените значения на ваши реальные:
- `TELEGRAM_BOT_TOKEN` - токен, полученный от BotFather
- `TELEGRAM_CHAT_ID` - ID чата/группы/канала, куда будут приходить уведомления

## 4. Создание таблицы leads в базе данных

Запустите миграцию для создания таблицы:

```bash
python scripts/create_leads_table.py
```

## 5. Перезапуск сервера

После настройки перезапустите FastAPI сервер:

```bash
# Если используете uvicorn напрямую
uvicorn app.main:app --reload

# Если используете systemd
sudo systemctl restart coffee-configurator
```

## 6. Проверка работы

1. Откройте конфигуратор на фронтенде
2. Нажмите "Запросить счёт"
3. Заполните форму и отправьте
4. Проверьте, что:
   - Сообщение пришло в Telegram
   - Запись создана в базе данных

## Формат уведомления в Telegram

Уведомление будет выглядеть так:

```
🔔 Новая заявка на счёт

👤 Имя: Иван Иванов
📞 Телефон: +7 999 123-45-67
✈️ Telegram: @ivan_ivanov
📧 Email: ivan@example.com

⚙️ Выбранная конфигурация:
• Кофемашина: Jura E8
• Каркас: Standard Frame
• Цвет каркаса: Черный
• Холодильник: Samsung RB37
• Терминал: Verifone V400m
• Цена: 250000 ₽
• Ссылка на OZON
```

## Troubleshooting

### Сообщения не приходят в Telegram

1. Проверьте правильность токена и chat_id в `.env`
2. Убедитесь, что бот добавлен в группу (если используется группа)
3. Проверьте логи сервера на наличие ошибок
4. Убедитесь, что сервер имеет доступ к интернету

### Ошибка "Forbidden: bot was blocked by the user"

Нажмите "Start" у бота в Telegram или разблокируйте его

### Ошибка "Bad Request: chat not found"

Неправильный Chat ID. Проверьте ID через метод getUpdates (см. шаг 2)

### Проверить доступность Telegram API

```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
```

Должен вернуться JSON с информацией о боте.
